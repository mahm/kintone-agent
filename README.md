## kintone Copilot demo

### Setup

python 3.10 or later is required.

```
$ export OPENAI_API_KEY=<your api key>
$ python -m venv venv
$ source venv/bin/activate
$ pip install -r requirements.txt
$ venv/bin/chainlit run chatbot.py
```